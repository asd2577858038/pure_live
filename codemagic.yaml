workflows:
  ios-trollstore:
    name: 构建 iOS IPA (最终修复版)
    max_build_duration: 60
    instance_type: mac_mini_m2
    
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    
    scripts:
      # 第1步: 清理与获取依赖
      - name: 第1步 - 清理与获取依赖
        script: |
          echo "🧹 开始深度清理..."
          flutter clean
          rm -rf ios/Pods
          rm -rf ios/Podfile.lock
          rm -rf ios/.symlinks
          
          echo "📦 获取 Flutter 依赖..."
          flutter pub upgrade
          flutter pub get

      # 第2步: 修正 Podfile (改用更安全的写法)
      - name: 第2步 - 修正 Podfile 配置
        script: |
          echo "🔧 正在修正 iOS Deployment Target..."
          cd ios
          
          # 使用 echo 逐行追加，避免 YAML 格式错误
          echo "" >> Podfile
          echo "post_install do |installer|" >> Podfile
          echo "  installer.pods_project.targets.each do |target|" >> Podfile
          echo "    target.build_configurations.each do |config|" >> Podfile
          echo "      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'" >> Podfile
          echo "      config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'" >> Podfile
          echo "    end" >> Podfile
          echo "  end" >> Podfile
          echo "end" >> Podfile
          
          echo "✅ Podfile 修正完成"

      # 第3步: 安装 iOS 依赖
      - name: 第3步 - 安装 Pods
        script: |
          echo "🥥 安装 CocoaPods..."
          cd ios
          pod repo update
          pod install --repo-update

      # 第4步: 编译 iOS
      - name: 第4步 - 编译 iOS
        script: |
          echo "🔨 开始编译..."
          flutter build ios --release --no-codesign

      # 第5步: 打包 IPA
      - name: 第5步 - 打包 IPA
        script: |
          echo "📦 正在打包..."
          PROJECT_ROOT=$(pwd)
          BUILD_DIR="$PROJECT_ROOT/build/ios/iphoneos"
          
          # 注意：这里的 if 必须保持缩进，
    
    publishing:
      email:
        recipients:
          - your-email@example.com # 记得改成你的邮箱
        notify:
          success: true
          failure: true
          fi
          cd ..
          echo "========================================="
      
      - name: 禁用代码签名
        script: |
          echo "========================================="
          echo "🔓 禁用代码签名"
          echo "========================================="
          
          cd ios
          cp Runner.xcodeproj/project.pbxproj Runner.xcodeproj/project.pbxproj.backup
          
          sed -i '' 's/CODE_SIGN_IDENTITY = "iPhone Developer"/CODE_SIGN_IDENTITY = ""/g' Runner.xcodeproj/project.pbxproj
          sed -i '' 's/CODE_SIGN_IDENTITY = "iPhone Distribution"/CODE_SIGN_IDENTITY = ""/g' Runner.xcodeproj/project.pbxproj
          sed -i '' 's/DEVELOPMENT_TEAM = [^;]*/DEVELOPMENT_TEAM = ""/g' Runner.xcodeproj/project.pbxproj
          
          cd ..
          echo "✅ 代码签名已禁用"
          echo "========================================="
      
      - name: 编译 iOS 应用
        script: |
          echo "========================================="
          echo "🔨 使用 xcodebuild 编译"
          echo "========================================="
          
          cd ios
          
          echo "开始编译 (这可能需要 5-10 分钟)..."
          xcodebuild -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -destination generic/platform=iOS \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=NO \
            -allowProvisioningUpdates \
            2>&1 | tee ../xcodebuild.log
          
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          
          cd ..
          
          if [ $BUILD_EXIT_CODE -eq 0 ]; then
            echo ""
            echo "✅ 编译成功"
            
            if [ -d "build/ios/Release-iphoneos/Runner.app" ]; then
              echo "✅ Runner.app 已生成"
              ls -lh build/ios/Release-iphoneos/Runner.app
            else
              echo "查找 Runner.app 位置..."
              find build/ios -name "Runner.app" -type d
            fi
          else
            echo ""
            echo "❌ 编译失败"
            echo ""
            echo "错误日志:"
            tail -200 xcodebuild.log
            exit 1
          fi
          echo "========================================="
      
      - name: 打包为 IPA
        script: |
          echo "========================================="
          echo "📦 打包 IPA"
          echo "========================================="
          
          APP_PATH=$(find build/ios -name "Runner.app" -type d | head -1)
          
          if [ -z "$APP_PATH" ]; then
            echo "❌ 未找到 Runner.app"
            echo "build/ios 目录结构:"
            find build/ios -type d -maxdepth 3
            exit 1
          fi
          
          echo "找到 Runner.app: $APP_PATH"
          
          BUILD_DIR=$(dirname "$APP_PATH")
          cd "$BUILD_DIR"
          
          echo "创建 Payload 结构..."
          mkdir -p Payload
          cp -r Runner.app Payload/
          
          echo "打包为 IPA..."
          zip -r -q PureLive-TrollStore.ipa Payload
          
          mv PureLive-TrollStore.ipa "$OLDPWD/"
          rm -rf Payload
          
          cd "$OLDPWD"
          
          if [ -f "PureLive-TrollStore.ipa" ]; then
            echo ""
            echo "✅ IPA 打包成功!"
            echo ""
            echo "📱 文件信息:"
            ls -lh PureLive-TrollStore.ipa
            echo ""
            echo "📦 IPA 内容:"
            unzip -l PureLive-TrollStore.ipa | head -30
            echo ""
            echo "🎉 可以用 TrollStore 安装了!"
          else
            echo "❌ IPA 未生成"
            exit 1
          fi
          echo "========================================="
      
      - name: 清理临时文件
        script: |
          if [ -f "pubspec.yaml.backup" ]; then
            echo "恢复原始 pubspec.yaml..."
            mv pubspec.yaml.backup pubspec.yaml
          fi
          
          if [ -f "ios/Runner.xcodeproj/project.pbxproj.backup" ]; then
            echo "恢复原始 Xcode 配置..."
            mv ios/Runner.xcodeproj/project.pbxproj.backup ios/Runner.xcodeproj/project.pbxproj
          fi
          
          echo "✅ 清理完成"
    
    artifacts:
      - PureLive-TrollStore.ipa
      - "*.log"
    
    publishing:
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: true
